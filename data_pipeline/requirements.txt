# Data Pipeline Requirements
# Install: pip install -r data_pipeline/requirements.txt

# ── FastAPI streaming server ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ── RAG / Embeddings ─────────────────────────────────────
chromadb>=0.4.24
sentence-transformers>=2.6.0

# ── NLP utilities ────────────────────────────────────────
tiktoken>=0.6.0        # Token counting for chunk sizing

# ── Document parsing ─────────────────────────────────────
python-docx>=1.1.0     # Parse .docx files

# ── Data utilities ───────────────────────────────────────
tqdm>=4.66.0           # Progress bars during embedding
